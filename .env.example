# Copie para .env (cp .env.example .env) e ajuste se necessário.
# O docker compose lê o .env automaticamente.

POSTGRES_USER=girassol
POSTGRES_PASSWORD=girassol
POSTGRES_DB=girassol
POSTGRES_PORT=5432
